import * as React from 'react'
import Layout from '../components/layout'

const CV = () => {
    return (
        <Layout pageTitle="Resume">
            todo
        </Layout>
    )
}

export default CV